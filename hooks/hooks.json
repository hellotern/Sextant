{
  "hooks": {
    "PermissionRequest": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // \"\"' | grep -q 'strip_frontmatter.py' && echo '{\"decision\":\"allow\",\"reason\":\"Sextant plugin: safe frontmatter stripping for skill loading\"}' || true"
          }
        ]
      }
    ]
  }
}
